//! Full pipeline orchestrator: dump → parse → extract → analyze.
//! Populated in Phase 6. Stub for now so Cargo.toml [[bin]] target resolves.

fn main() {
    eprintln!("full_pipeline: not yet implemented (Phase 6)");
    std::process::exit(1);
}
